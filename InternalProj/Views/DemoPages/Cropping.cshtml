@{
    var current = "cropping";
    var depts = (Context.Session.GetString("UserDepartments") ?? "")
                .ToLower()
                .Split(',', StringSplitOptions.TrimEntries);
}

@if (depts.Contains(current))
{
    <p>You are from @current department.</p>
}

@* <h1>Hi this is Tushar</h1> *@