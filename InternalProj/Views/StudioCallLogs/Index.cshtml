@* @model IEnumerable<InternalProj.Models.StudioCallLog>

@{
    ViewData["Title"] = "Call Logs";
    var today = DateTime.Today;
}

<h3 class="text-center text-2xl font-extrabold mb-8 text-gray-900">Call Logs</h3>

<p>
    <a asp-action="Create" class="inline-block px-4 py-2 bg-gray-300 text-gray-800 rounded-md hover:bg-gray-400 transition">
        Create New
    </a>
</p>

<div class="overflow-x-auto">
    <table class="min-w-full border border-gray-300 rounded-lg mt-4" id="callLogTable">
        <thead class="bg-gray-100">
            <tr>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().StudioName)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().Phone)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().Region)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().CallTime)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().Description)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    Updated Call Day
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                bool isToday = item.UpdatedCallTime?.Date == today;
                <tr data-callid="@item.CallId" class="@(isToday ? "bg-green-100" : "") hover:bg-gray-50 cursor-default">
                    <td class="studio-cell px-4 py-2 border-b border-gray-200 text-indigo-700 font-medium cursor-pointer">
                        @Html.DisplayFor(modelItem => item.StudioName)
                    </td>
                    <td class="px-4 py-2 border-b border-gray-200">@Html.DisplayFor(modelItem => item.Phone)</td>
                    <td class="px-4 py-2 border-b border-gray-200">@Html.DisplayFor(modelItem => item.Region)</td>
                    <td class="px-4 py-2 border-b border-gray-200">@item.CallTime.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</td>
                    <td class="px-4 py-2 border-b border-gray-200">@Html.DisplayFor(modelItem => item.Description)</td>
                    <td class="px-4 py-2 border-b border-gray-200">@item.UpdatedCallTime?.ToLocalTime().ToString("yyyy-MM-dd")</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Pagination -->
<div class="pagination-container mt-6 flex justify-center">
    <nav aria-label="Page navigation">
        <ul class="inline-flex -space-x-px">
            @for (int i = 1; i <= (int)ViewBag.TotalPages; i++)
            {
                var active = i == (int)ViewBag.CurrentPage;
                <li>
                    <a href="@Url.Action("Index", new { page = i })"
                       class="px-4 py-2 border border-gray-300 text-gray-600 hover:bg-gray-200 transition first:rounded-l last:rounded-r @(active ? "bg-gray-600 text-white font-semibold" : "bg-white")">
                        @i
                    </a>
                </li>
            }
        </ul>
    </nav>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#callLogTable').on('dblclick', '.studio-cell', function () {
                const callId = $(this).closest('tr').data('callid');
                if (callId) {
                    window.location.href = '/StudioCallLogs/Edit/' + callId;
                }
            });
        });
    </script>
} *@




@model IEnumerable<InternalProj.Models.StudioCallLog>

@{
    ViewData["Title"] = "Call Logs";
    var today = DateTime.Today;
}

<style>
    .pagination-container nav ul li a {
        min-width: 2.5rem;
        text-align: center;
    }
</style>

<h3 class="text-center text-2xl font-extrabold mb-8 text-gray-900">Call Logs</h3>

<p>
    <a asp-action="Create" class="inline-block px-4 py-2 bg-gray-300 text-gray-800 rounded-md hover:bg-gray-400 transition">
        Create New
    </a>
</p>

<div class="overflow-x-auto rounded-lg border border-gray-300">
    <table class="min-w-full text-left text-gray-700" id="callLogTable">
        <thead class="bg-gray-100">
            <tr>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().StudioName)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().Phone)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().Region)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().CallTime)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    @Html.DisplayNameFor(model => model.First().Description)
                </th>
                <th class="px-4 py-2 border-b border-gray-300 text-left text-gray-700 font-semibold">
                    Updated Call Day
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                bool isToday = item.UpdatedCallTime?.Date == today;
                <tr data-callid="@item.CallId" class="@(isToday ? "bg-green-100" : "") hover:bg-gray-50 cursor-default">
                    <td class="studio-cell px-4 py-2 border-b border-gray-200 text-indigo-700 font-medium cursor-pointer">
                        @Html.DisplayFor(modelItem => item.StudioName)
                    </td>
                    <td class="px-4 py-2 border-b border-gray-200">@Html.DisplayFor(modelItem => item.Phone)</td>
                    <td class="px-4 py-2 border-b border-gray-200">@Html.DisplayFor(modelItem => item.Region)</td>
                    <td class="px-4 py-2 border-b border-gray-200">@item.CallTime.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</td>
                    <td class="px-4 py-2 border-b border-gray-200">@Html.DisplayFor(modelItem => item.Description)</td>
                    <td class="px-4 py-2 border-b border-gray-200">@item.UpdatedCallTime?.ToLocalTime().ToString("yyyy-MM-dd")</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Pagination BELOW table, outside the overflow container -->
@* <div class="pagination-container mt-40 flex justify-center max-w-full lg:max-w-7xl mx-auto px-2 lg:px-8">
    <nav aria-label="Page navigation">
        <ul class="inline-flex -space-x-px">
            @for (int i = 1; i <= (int)ViewBag.TotalPages; i++)
            {
                var active = i == (int)ViewBag.CurrentPage;
                <li>
                    <a href="@Url.Action("Index", new { page = i })"
                       class="px-4 py-2 border border-gray-300 text-gray-600 hover:bg-gray-200 transition first:rounded-l last:rounded-r @(active ? "bg-gray-600 text-white font-semibold" : "bg-white")">
                        @i
                    </a>
                </li>
            }
        </ul>
    </nav>
</div> *@

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#callLogTable').on('dblclick', '.studio-cell', function () {
                const callId = $(this).closest('tr').data('callid');
                if (callId) {
                    window.location.href = '/StudioCallLogs/Edit/' + callId;
                }
            });
        });
    </script>
}
